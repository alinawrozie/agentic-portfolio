# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
infra/build/
# Note: .terraform.lock.hcl is intentionally NOT ignored - commit it for
# reproducible provider versions across your machine and CI.

# OS / editor noise
.DS_Store
*.swp
.vscode/
.idea/
